#!/bin/bash
set -e

echo "[5/5] 检查 Docker 输出..."

# OUTPUT=$(docker run --rm hello-world-docker)

# echo "容器输出：$OUTPUT"

# if [[ "$OUTPUT" == *"Hello World"* ]]; then
#     echo "验证成功：成功通过 Docker 输出 Hello World!"
# else
#     echo "验证失败：容器未正确输出。"
#     exit 1
# fi
